#!/usr/bin/env ruby

lib = File.dirname(__FILE__) + '/../lib'

require 'rubygems'

if File.file?(lib + '/arcabouco/version.rb')
  $LOAD_PATH << lib
else
  gem 'arcabouco'
end

require 'arcabouco'

Arcabouco::Application.run
